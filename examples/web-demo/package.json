{
  "name": "@tracerability/example-web-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5174",
    "preview:prepare": "pnpm build && pnpm sourcemap:upload",
    "sourcemap:upload": "traceability sourcemap upload --project demo-web --dist ./dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@tracerability/monitor": "workspace:*"
  },
  "devDependencies": {
    "@sentry/vite-plugin": "^5.4.0",
    "@tracerability/cli": "workspace:*",
    "vite": "^5.4.0",
    "vitest": "^2.1.9"
  }
}
